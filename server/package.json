{
  "name": "samurai-kirby-server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "discord": "bun run --watch src/discord-serve.ts"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
