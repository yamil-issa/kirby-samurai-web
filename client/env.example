# Copy this file to .env.local (same folder). Only PUBLIC_ prefixed vars are
# ever inlined into the client bundle (see bunfig.toml) — never put the
# client secret here, it belongs only in server/.env.

PUBLIC_DISCORD_CLIENT_ID=your_application_client_id
