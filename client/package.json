{
  "name": "samurai-kirby-client",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot index.html",
    "build": "bun build ./index.html --outdir=dist --env=PUBLIC_* --minify"
  },
  "dependencies": {
    "@discord/embedded-app-sdk": "^2.5.0"
  }
}
